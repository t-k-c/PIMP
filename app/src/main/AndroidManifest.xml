<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pi.novobyte.com.pimp">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity">

        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".PartnerViewActivity" />
        <activity android:name=".PartnerContact" />
        <activity android:name=".AddingItems" />
        <activity android:name=".AddSite" />
        <activity android:name=".MySites" />
        <activity android:name=".AddNews" />
        <activity
            android:name=".PostViewActvity"
            android:label="@string/title_activity_post_view_actvity"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">

            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="pi.novobyte.com.pimp.MainActivity" />
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".LocationManager"
            android:label="@string/title_activity_location_manager" />
        <activity android:name=".SiteListActivity" />
        <activity android:name=".LikedItems" />
        <activity android:name=".add_item" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ItemViewActivity"></activity>
    </application>

</manifest>